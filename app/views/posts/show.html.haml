%h1= @post.title
%p= @post.link
%p= @post.description

= link_to "Edit", edit_post_path(@post)
= link_to "Destroy", post_path(@post), method: :delete, data: { confirm: "Take a breath....do you really want to do this?" }
= link_to "Home", root_path
